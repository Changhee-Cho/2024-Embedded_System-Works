#include "getch.h"

char getch();

int main(){
    char key;
    if(key=getch()){
        switch(key){
            case 'q':
                break;
            case 'w':
                break;
        }
    }
}